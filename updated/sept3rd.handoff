produces wrong/blank output.
Decide on the API-key vs payment-signature duplication — two payment paths now coexist in requirePayment. Decide if both are actually wanted long-term, or if the API-key model should become the primary one (cleaner for repeat users) with the per-signature model deprecated.
Harden the API-key issuance scheme — currently a payer's own wallet address doubles as their key_hash, which is a simple placeholder, not a securely generated/rotatable key. Worth designing a real key-issuance flow.
The original Session 1 goal is still open: no genuine end-to-end test with a REAL mainnet SOL transfer has ever been done — only hand-crafted synthetic webhook payloads. Needs either a funded mainnet wallet or switching the Helius webhook to devnet + faucet funds for a lower-stakes first real test.
Consider Google Secret Manager for DATABASE_URL/HELIUS_WEBHOOK_SECRET instead of plaintext --set-env-vars (noted in session 1, still not done).
Rotate secrets eventually — DB password and webhook secret have been posted in plaintext across two long chat sessions now.Security/Hygiene Notes for Whoever Picks This UpDB password (1Qaz2Wsx..) and webhook secret (hw_7f3kD9mQxL2vN8pRz4tYcB6sJ1aE5wXu) have been in plaintext across a long chat session and multiple terminal pastes. Consider rotating both once things stabilize, especially before wider collaboration or if this repo/chat log is ever shared further.Test/scratch files (test-payload.json, test-db.ts, test-webhook.py, underpaid.json, paid.json, etc.) were created and mostly cleaned up during the session but check git status/ls for stragglers before a clean commit.
